/** Production site origin for metadata, sitemap, and structured data. */
export const SITE_URL = "https://www.orivona.com" as const;

/** Canonical homepage URL (www, trailing slash). */
export const SITE_CANONICAL_URL = `${SITE_URL}/` as const;
