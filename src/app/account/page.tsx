import { AccountProfileView } from "@/src/components/app/AccountProfileView";

export const metadata = {
  title: "Hesabım | ORIVONA",
};

export default function AccountPage() {
  return <AccountProfileView />;
}
